name = "tomcp"
main = "src/index.ts"
compatibility_date = "2024-12-01"
account_id = "ec62a93ac5823c4621864bda8abb2be4"  # Ami3466@gmail.com

# Workers AI - free tier, no API key needed
[ai]
binding = "AI"

# Custom domain (already configured in dashboard)
# routes = [
#   { pattern = "tomcp.org", custom_domain = true }
# ]
